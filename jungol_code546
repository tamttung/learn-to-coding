//jungol_code546

#include<stdio.h>

int main() {
	int n,i,score,sum=0;
	double avg=0;
	
	scanf("%d",&n);

	for(i=1; i<=n; i++) {
		scanf("%d",&score);
		sum+=score;
	}
	avg=(double)sum/n;
	printf("avg : %.1lf\n",avg);
	
	if(avg>=80) {
		printf("pass");
	} else {
		printf("fail");
	}

	return 0;
}
